#include <iostream>
#include <cmath>

int main() {

    // array = data structure that can hold multiple values 
        // add a square and the {};
    // array must have only the same data type


    std::string car[] = {"Covette", "Mustang", "Viper"};

    car[0] = "Bugatti";
    

    // can create an empty array and set the size of the arrary 
    int age[5];

    age[0] = 35;
    age[4] = 23;


    std::cout << car[0];
    std::cout << "\nYour age is: " << age[4]; 









    return 0;
}